# 查询结果导出验收记录

日期：2026-08-30

状态：自动化实施验收通过；桌面已补验五种格式的保存及顶部/右键 INSERT 实际复制，完整交互清单仍有未验证项。XLSX 内容核对通过，但现有默认列宽的可读性问题待单独改进。下文按尝试顺序保留证据，不把自动化测试替代人工观察结论。

范围：SQL 编辑器 XLSX/CSV/SQL/HTML/XML 和复制 INSERT；不含整表、迁移、Redis、pg_dump。

## 自动化证据

- 完整 non-headless：`$env:JAVA_TOOL_OPTIONS='-Djava.awt.headless=false'; ./gradlew clean test --no-daemon --console=plain`，退出码 0，`BUILD SUCCESSFUL`。
- JUnit XML（`build/test-results/test/TEST-*.xml`）统计：135 个 suite、1196 个测试、1193 通过、0 failures、0 errors、3 skipped。该统计由 XML 属性实际求和，不是 Gradle `UP-TO-DATE` 推断。
- 主代理在任务 8 提交 `e74b29c` 后独立重跑上述 `clean test`，退出码 0，用时 25 秒；再次读取 XML 得到相同统计。不是仅转述子代理的测试报告。
- 在 `7f6660f` 后完成剪贴板与其他格式续验，再次运行同一完整 non-headless `clean test`，27 秒退出码 0；重新汇总 JUnit XML：135 suites、1196 tests、1193 passed、0 failures/errors、3 skipped。本次未修改生产/测试代码，统计来自新运行而非旧报告。
- skipped 均为明确禁用的 live 集成测试，未使用公司连接或凭据：Redis 五类型 smoke 缺少 `DATACUBE_REDIS_HOST` 和 `DATACUBE_REDIS_PASSWORD`；Oracle 与 PostgreSQL Schema Diff smoke 缺少显式写入 gate 和完整 provider 环境。
- 本机符号链接拒绝回归实际运行通过；未因符号链接能力限制或 headless 跳过测试。上述三项 live 测试未计入通过数。
- 文件失败注入覆盖旧文件字节不变、仅本次临时文件清理、关闭流失败、目标锁冲突、清理失败、目录/链接目标拒绝和不支持原子移动。

- `git diff --check` 在本轮自动化证据后通过；编译器仍报告既有 `SqlEditorResultFilterContractTest` 未检查操作提示（非失败）。

## 桌面验收

- 默认范围、全量选项、零匹配、隐藏/重排列、升降序、刚输入搜索就导出。
- 同样的数据分别从顶部和右键复制 INSERT，取消不写剪贴板。
- 行截断与预览值分别显示，SQL 拒绝特殊值，非 SQL 需显式确认。
- 深色/浅色、最小可用窗口、键盘 Tab/Enter/Esc 和长提示不遮挡按钮。
- 仅使用合成数据、临时目录，不打开公司数据库或用户已有导出文件。

### 首次尝试：控制环境拒绝访问

2026-08-30 首次尝试未完成交互及视觉验收，记录如下：

- 通过本地临时 harness 的 `runExportSmoke` 启动 SQL 编辑器，不配置数据库连接；无公司连接、历史 SQL 或已有导出文件访问。
- 桌面无障碍树确认窗口“DataCube 导出验收 · 合成数据”存在，表内有 3 行、3 列合成数据，“导出结果”和“复制INSERT”入口可见。
- 工具截图为黑屏；重新绑定窗口后再次尝试激活，仍返回 `GetCursorPos failed: 拒绝访问。 (0x80070005)`。没有执行任何点击、键盘、剪贴板或保存操作，不能据无障碍树判定主题、排版或交互通过。
- 已核对进程命令确为本次 `ExportSmokeLauncher` 后，仅停止该合成 Java 进程，未停止其他 Java 进程。此主动停止导致 `runExportSmoke` 返回退出码 1（子进程 -1），不计为成功验收，也不是完整测试套件的失败。
- harness 启动有 JavaFX unnamed-module 警告；它使用单独的 classpath 启动方式，该次运行不证明正式打包启动已验证。
- 仍需在可交互的 Windows 桌面补验上述范围切换、实际文件输出、主题、小窗口及键盘操作。

### 首轮续验（记录于 7f6660f）：核心路径通过，部分交互待补验

2026-08-30 用户认可继续后，重新启动独立合成窗口。本轮画面可见，通过 computer-use 操作既有 UI；未修改生产代码或测试代码，未重跑完整测试套件，上述自动化统计仍对应上一轮 `e74b29c` 后的运行。

已直接观察或核对：

- 深色初始 CSV 确认默认“当前筛选结果（当前排序）”，显示 3 行、3 列；明确提示两种范围都使用当前可见列，全部已加载不代表数据库全量且不会重新查询。点击取消后恢复编辑器。
- 搜索 `Ada` 后显示 2 / 3 行，按“分数”升序排列为 8、12；CSV 确认显示 2 行、3 列，不因只选中一个单元格而缩成一行。
- 使用系统保存对话框在本轮独占临时目录新建 `filtered-current.csv`，界面显示已导出。通过 PowerShell `Import-Csv` 加显式断言验证恰有两行、顺序为 8/12、列为姓名/分数/创建时间，且保留 `2026-08-30T10:20:30.123456789`。未覆盖已有文件。
- 搜索 `NO_MATCH_SYNTHETIC` 后为 0 / 3 行；导出仍默认当前筛选，显示“当前筛选结果为 0 行”，继续按钮禁用，没有自动扩大范围。
- 加载 700 字符 CLOB 且标记结果截断的合成样例后，CSV 确认分别显示行截断提示和 1 个特殊值单元格提示；未确认时继续禁用，显式勾选后启用。取消后再次打开确认，勾选状态未保留。
- 顶部“复制INSERT”对该 CLOB 显示无法无损生成 INSERT，继续禁用，且无勾选绕过入口；仅取消，没有提交剪贴板写入。
- 深色常规窗口以及浅色 900×620 紧凑窗口中，预览/截断文本与继续/取消按钮完整可见，无遮挡。此观察仅覆盖当前样例，不代表全部 DPI/分辨率组合。
- 通过标题栏正常关闭合成窗口，`runExportSmoke` 最终退出码 0、`BUILD SUCCESSFUL`；没有强制终止进程。运行期间的 JavaFX unnamed-module 警告仍属于该临时 classpath harness。

实际 CSV 内容（UTF-8；这里只展示文本，不表示换行字节规范）：

```csv
姓名,分数,创建时间
Ada,8,2026-08-29T09:01:02
Ada,12,2026-08-30T10:20:30.123456789
```

文件 SHA-256：`33914EB1DCCF653030D83A91CC0801B1BC9CA2674A4D55F0DF325EE6C9534385`。文件保留于本机本轮创建的 `datacube-export-smoke-2371476783416274294` 临时目录，未加入仓库。只导出了合成数据；没有打开公司连接、执行数据库查询或读取已有导出内容。

同轮第二次启动追加验收：

- 拖动“创建时间”列到首个数据列位置，再将“分数”设为降序，画面顺序为 25、12、8。CSV 确认显示 3 行、3 列，通过系统保存对话框新建 `reordered-desc.csv`。
- 读取实际文件并断言列顺序为创建时间/姓名/分数、不含 `#`，行顺序为 25/12/8，中文及 `& <演示>` 保留，时间仍含纳秒。两份 CSV 均在收尾时重读，表头、行顺序、时间与 SHA-256 断言再次通过。
- 在 CLOB 预览样例中选择“导出结果 → SQL 插入脚本 (.sql)”，确认框显示 1 个特殊值单元格、无法无损生成 INSERT，继续按钮禁用；点击取消返回编辑器，没有进入保存流程或创建 SQL 文件。
- 右键菜单可见“复制为 INSERT 语句”，但选择该项被桌面工具安全检查拒绝，理由是可能覆盖已有剪贴板且未获明确授权。未绕过拒绝，使用 Escape 关闭菜单；这只证明主窗口右键菜单可以退出，不代表独立确认弹窗的键盘验收通过。
- 第二次合成窗口亦通过标题栏正常关闭，`runExportSmoke` 退出码 0、`BUILD SUCCESSFUL`，随后应用列表中不再存在该验收窗口。两次启动均未配置数据库连接。

第二份实际 CSV：

```csv
创建时间,姓名,分数
,张三 & <演示>,25
2026-08-30T10:20:30.123456789,Ada,12
2026-08-29T09:01:02,Ada,8
```

文件 SHA-256：`4D0A14D6A01E4AA56054C1E272AE9D5666DE2AFDD972D5638579E605BBA0BA96`，保留于本机新建的 `datacube-export-smoke-2248373429399092510` 临时目录，未加入仓库。

截至 `7f6660f` 的未验证项与工具限制（后续补验见下一节）：

- 已观察到“全部已加载行（加载顺序）”选项，但未可靠完成切换及切换后的保存；其桌面行为仍待补验，不由既有自动化测试替代。
- Windows 控制工具不能稳定提供独立 JavaFX 弹窗对象及焦点：`list_windows/list_apps` 未返回该弹窗，主窗口无障碍焦点与视觉焦点不一致。`set_value` 返回 CacheRequest 属性错误；保存按钮的子节点索引也曾失效，之后通过新截图坐标完成保存。这些现象记录为工具限制，不据此认定产品缺陷。
- Escape/Tab 未在独立弹窗中可靠验证；不将尝试过按键记为成功。
- 列隐藏、在防抖到期前立即导出、标量 SQL 文件成功保存及 XLSX/HTML/XML 文件保存的桌面路径未补验。列重排、升降序和 SQL 特殊值阻止已按上文实际验证，不能据此扩写为所有格式通过。
- 右键 INSERT 的确认流程和实际剪贴板输出仍未验证；安全拒绝不等于产品失败。若后续要验证真实复制，须先取得允许覆盖当前剪贴板的明确授权。顶部 INSERT 本轮只验证 CLOB 阻止，没有测试实际复制成功。

### 第二轮续验：已获剪贴板授权，补验复制与其他格式

2026-08-30 用户针对“是否允许用合成 INSERT 文本覆盖当前剪贴板”明确认可继续。本轮仅使用独立合成窗口与新建目录 `datacube-export-smoke-16150016981682116109`；未读取原有剪贴板内容、未访问数据库，未执行生成的 INSERT。

- 顶部“复制INSERT”默认当前范围，确认 3 行、3 列后显示“已复制 3 条 INSERT 语句”。将实际剪贴板粘贴到该无连接编辑器，只核对、不执行。
- 右键“复制为 INSERT 语句”同样先确认 3 行、3 列，虽然仅选中一个单元格，实际粘贴仍有三条；按编辑器段落读取文本，与顶部结果逐条相等。两次均保留中文、`NULL`、纳秒时间。
- 将结果搜索改为 `Ada`（2 / 3 行），打开顶部复制确认后点击取消；再次粘贴并比较，剪贴板仍是原三条语句，没有被两行结果替换。该观察覆盖范围确认取消，不扩写为所有取消阶段。
- 搜索 `Ada` 后通过 SQL 文件入口实际保存 `filtered-scalar.sql`，逐行断言只有 12、8 两条 Ada INSERT，包含纳秒时间、不含被过滤的中文行。
- 恢复三行标量样例后，分别从 HTML/XML/XLSX 入口确认 3 行、3 列并通过系统保存对话框创建新文件。HTML/XML 的中文 `&` 和尖括号被转义，XML 解析后得到原字符串；三行顺序、空值表示和纳秒时间核对通过。
- 使用 Spreadsheets 技能只读导入原始 XLSX，检查 `Sheet1!A1:C4`：表头为姓名/分数/创建时间，分数是数值 12/25/8，中文完整，空单元格为 null，时间字符串含完整纳秒；错误值扫描无匹配。未重新导出或修改 XLSX。
- XLSX 渲染显示长中文及时间受默认列宽限制，故不将其排版记为通过。只读检查实际 OOXML：没有 `<cols>` 列宽定义，也没有 `xl/styles.xml`；`XlsxWriter.writeSheet` 直接写入 `<sheetData>`。该共享 writer 最近修改为 `418f399`，相对本阶段设计基线 `c811802` 无变更。确认是现有输出缺少排版定义，而非本轮数据截断；未在 Microsoft Excel 应用中复验最终显示。
- 上述排版问题涉及查询结果与整表共用 writer，留作单独产品改进，不在本轮验收中附带修改共享实现。
- 通过标题栏正常关闭本次合成窗口，`runExportSmoke` 退出码 0、`BUILD SUCCESSFUL`；再次列出应用时验收窗口不存在。未覆盖已有导出文件，临时验收文件保留本机、不加入仓库。

实际复制的三条文本：

```sql
INSERT INTO demo_export (姓名, 分数, 创建时间) VALUES ('Ada', 12, '2026-08-30T10:20:30.123456789');
INSERT INTO demo_export (姓名, 分数, 创建时间) VALUES ('张三 & <演示>', 25, NULL);
INSERT INTO demo_export (姓名, 分数, 创建时间) VALUES ('Ada', 8, '2026-08-29T09:01:02');
```

本轮实际文件 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| filtered-scalar.sql | `65982E4892EC5BC42A4881524A99FF19274019A325697887E6E45EACE506C82F` |
| scalar.html | `1C44AEB460DCBF76EE8A712D59E5C2BCBB31FA3BA030FF35033779BA2F436881` |
| scalar.xml | `AB446EE02C53235C1E7E5E0508BE9748AE851F40A40B2285CF72D07745CDCCB7` |
| scalar.xlsx | `0F050AC50576A1A6790D86A526D2ED522BCD90B284CE3DC5474E2D8E08177DF2` |

剩余边界：

- 全部已加载切换及其保存、列隐藏、独立弹窗 Tab/Enter/Esc、防抖到期前立即导出仍未完成桌面验证。本轮弹窗打开时再次调用 `list_windows/list_apps`，仍只返回主窗口，不把主窗口键盘操作记为弹窗通过。
- 五种格式均有实际保存证据，但没有穷举全部“范围 × 格式 × 值类型”组合；XLSX 数据通过与排版待改进分开记录。
- 原剪贴板已按用户授权被合成 INSERT 替换；没有读取、备份或恢复用户原内容。

### P0.2 续验：隐藏列入口缺失

同日基于 `edce8a2` 的独立验收 worktree 再次运行无数据库连接的合成窗口，观察到 Ada 筛选、8/12 升序及 CSV 确认 2 行、3 列；未成功切换全部已加载范围，独立弹窗仍不能由控制工具可靠定位，未创建导出文件。

本次右键表头和源码检查确认查询结果表没有显示/隐藏列入口，原“列隐藏待桌面补验”的前提不成立。`SqlEditorResultFilterContractTest` 直接设置列可见性的覆盖只证明内部契约，不证明现有 UI 已提供该功能。是否补充入口需先确认设计；不通过修改夹具来假装用户已操作通过。完整过程、正常退出与配置兼容证据见 [P0.2 发布验收记录](2026-08-30-release-acceptance.md)。

## 交付限制

- 最终全分支代码审查范围 `c811802..e74b29c`：无未解决 Critical、Important 或 Minor 发现。此前原子移动测试断言、空截断提示占位和任务拒绝覆盖新状态的问题均已修复并复审；代码审查通过不代替桌面验收。

- 未验证项必须保留说明，不用自动化测试替代人工观察结论。
- 不保证跨库 SQL 恢复、不改变 CSV 公式策略、不承诺恶意外进程竞态或断电持久化。
- 初始 CLOB 捕获会流式读取并对完整内容做指纹，仅保留有界预览；导出不再 JDBC 重读。大字段的 live 数据库延迟未验证。
- 本阶段不自动推送、合并或打 tag。
