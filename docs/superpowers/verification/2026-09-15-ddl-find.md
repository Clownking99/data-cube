# 只读 DDL 查找验收

基线 main `45b1929`，独立分支 `codex/ddl-find`。设计见[只读 DDL 查找](../specs/2026-09-15-ddl-find.md)。不访问真实数据库/业务文件或用户 `.testagent/`，不推送、不打 tag。

## 需求与证据

测试技能用于入口、异步发布、只读边界及窄窗布局的直接回归；按项目约定不引入额外测试流水线工件。

| 需求 | 证据 |
| --- | --- |
| 工具栏/Ctrl+F 打开、Esc 收起，Ctrl+H 无替换 | `DdlViewPaneFindTest.toolbarAndShortcutOpenReadOnlyFindWithoutReplacementControls` |
| 大小写/整词、正反定位、Unicode 字面词、选区预填、不重复读取 | `actualViewerSearchCountsNavigatesAndKeepsDdlUnchangedWithoutRefetch` |
| 加载从首行展示；加载前搜索，加载后刷新计数但不自动选中 | `actualViewerSearchCountsNavigatesAndKeepsDdlUnchangedWithoutRefetch`、`loadingDdlRefreshesAnAlreadyOpenSearchWithoutAutoSelecting` |
| 空/失败加载只搜索已显示内容 | `emptyAndFailedLoadsRemainReadOnlyAndSearchOnlyDisplayedText`（2 种） |
| 后台关闭取消读取、FX 清理幂等、共享 runner 可继续工作 | `backgroundCloseCancelsLoadThenFxFinalizerPreventsFurtherSearchWithoutClosingRunner` |
| 480 窄窗明暗主题按钮与聚焦提示可见 | `narrowThemesKeepFindControlsAndFocusedPromptVisible`（2 种） |
| 普通/只读模式共享循环定位、空词/超长、过期成功/失败及截断反馈 | `SqlFindBarTest.navigationWrapsBothWaysWithoutEditingTheScript`、`changingCaseAndClearingOrOversizingQueryInvalidateActions`、`editingCancelsOldScanAndLateSuccessCannotSelectStaleOffsets`、`hideAndCloseSuppressLateCallbacksAndDoNotStartHiddenWork`、`truncationAndFailureFeedbackAreExplicitAndNeverExposeBackendDetails`（各 2 种） |
| 即使正文变为可编辑，只读模式仍不能展示替换入口 | `readOnlyModeCannotExposeReplacementEvenWhenEditorBecomesEditable` |
| 原编辑器替换行为不回退 | 既有 `SqlReplaceBarTest`、`SqlEditorReplaceIntegrationTest` 联跑 |

## 自动验证

设置 `JAVA_TOOL_OPTIONS=-Djava.awt.headless=false`，使用 Gradle wrapper `--no-daemon --console=plain`。

1. 入口 RED：47 秒 exit 1，明确缺少 DDL 查找按钮。
2. 接入后初步查找/替换回归：15 秒 exit 0。
3. 扩充 49 项联跑发现 2 项窄窗失败：长标题导致 FlowPane 最小宽度撑开，按钮位于 x=530，超出 480 视口。先明确测试视口为 480，再将生产工具栏最小宽度设为 0，使其按可用宽度换行；保留越界断言。
4. 最终定向联跑：28 秒 exit 0，49 项全部通过。

```text
test --tests com.datacube.fx.DdlViewPaneFindTest
     --tests com.datacube.fx.DdlViewPaneLifecycleTest
     --tests com.datacube.fx.SqlFindBarTest
     --tests com.datacube.fx.SqlReplaceBarTest
     --tests com.datacube.fx.SqlEditorReplaceIntegrationTest
     --no-daemon --console=plain
```

5. 首轮全量 `clean :buildSrc:test test jpackageImage -PappVersion=0.0.0`：3 分 29 秒 exit 0，2,656 项中 2,653 通过、3 项既有 live 跳过；buildSrc 8 项通过。
6. 首轮真实镜像确认 Ctrl+F、输入提示、Enter 循环定位及失焦正文高亮可用，但发现长 DDL 初始停在尾部。补首行断言 RED：16 秒 exit 1（期望 caret=0，实际 58），成功/失败显示路径统一复位首行。49 项定向重跑：44 秒 exit 0。首轮验收副本正常关闭后精确 exe 进程数 0。

7. 最终修订再次执行相同全量 clean/test/jpackage 命令：3 分 20 秒 exit 0，2,656 项中 2,653 通过、3 项既有 live 跳过、0 失败/错误；buildSrc 8 项成功报告不变（本次 up-to-date）。净新增 14 项回归。现有 unchecked、JAVA_TOOL_OPTIONS 工具链探测及 JEP 493 提示仍存在，不宣称无警告。

## 最终镜像桌面验收

computer-use 操作最终生产镜像的独立副本 `build/ddl-final-desktop-image/DataCube`，仅副本以 `--patch-module` 载入 `DdlFindDesktopFixture`，使用显式 `build/ddl-find-desktop-profile`。调用真实 DdlViewPane 和 FxTaskRunner，DDL 由测试常量提供，不实例化数据库服务或 AppShell，不读取真实连接或业务 SQL。窗口最初捕获失败一次，重新枚举并激活返回的窗口后恢复。

- 暗色宽窗初始直接显示第 1 行 CREATE TABLE；长 DDL 不再默认停在末尾。
- 暗色 480 窄窗长标题下「复制」「查找」换行可见，Ctrl+F 打开，聚焦提示清楚。输入 `id` 不移动正文，Enter 定位首项 1/6，Shift+Enter 循环到第 97 行 6/6 并明确提示回到末尾；焦点仍留在查询框，正文高亮可见。
- 勾选整词后共 4 处，保留第 97 行选区；切换亮色仍可见。再启用大小写，Ctrl+F 保留选项并带入选区，Enter 返回 1/3。
- 亮色窄窗输入 `missing_marker` 显示无匹配，定位按钮禁用，Enter 不移动原选区。Esc 收起查找并保留正文选区；鼠标「查找」重开带入 `id`，Backspace 清空后聚焦提示仍可见。
- 正常关闭最终副本后，精确 exe 路径进程数为 0；隔离 profile 含主题设置及 JavaFX 自动解压的原生运行库缓存，不生成 SQL 文件或保存连接。自动测试负责加载竞态、取消、过期回调和限额等路径，不将它们全部称为桌面实测。

生产入口仍为 `com.datacube/com.datacube.DataCubeFx`，无 fixture patch/profile 参数；runtime 含 DdlViewPane、不含 DesktopFixture 或 DraftConnectionProbe。最终 modules SHA-256：`A5959B0F5020D423F535AE8A1732C3E44B76BA0981505EF2093F8B67FB3EB86F`。开发版本 0.0.0，不代表正式发布、安装升级、真实数据库或远端 CI 验收。

## 本地 main 集成

实现 `35d947c` 已快进 main；合并前跟踪文件及暂存区干净，只保留用户 `.testagent/`，未读取、修改或暂存该目录。未推送、未打 tag。

合并后运行上方同一 49 项定向命令，追加 `jpackageImage -PappVersion=0.0.0`：59 秒 exit 0，49 项全部通过，无失败/错误/跳过。全量报告保留在独立 worktree，main 测试报告为定向验证。

main 预览为 `build/jpackage/DataCube/DataCube.exe`，入口及不含测试夹具检查再次通过。modules SHA-256 为 `89178C6841C643A8A6F07A1DC18998BD902E07FB71825D40ABCBD7DBACAC4FCF`；分别提取两个 runtime 的 `com.datacube` 模块，各 804 项，唯一差异是 theme-base.css 的 Git 检出 CRLF 与 worktree LF。换行规范化后内容相同，不声称两个镜像逐字节一致。
