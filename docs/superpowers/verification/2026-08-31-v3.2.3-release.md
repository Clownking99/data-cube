# v3.2.3 发布前验证

维护者在已知对话框键盘验收尚未完成后，明确要求“提交，并打个tag，然后推送”。本轮据此整合并准备发布，不将发布授权等同于P2全部验收通过。

## 提交与版本

- 发布前远端main为`c8c53aab443181b817fad0e75dac0cfa182e8694`，最新且唯一tag为`v3.2.2`；`v3.2.3`未占用。
- 本地main从`109df89`快进到`1ed9fe58fb8e2f4ce1d44580e39c85a8e08c6567`。相对远端的生产变更仅为SqlAutoComplete焦点修复及其7项测试；其余是验收/设计记录。
- 计划在本记录提交上创建附注tag `v3.2.3`，原子推送main及该tag，不改动或删除旧tag，不强推。使用单次命令代理127.0.0.1:7897，不修改持久Git代理配置。
- 现有tag工作流将版本3.2.3传入Gradle，并在远端测试通过后构建便携包/安装程序和发布Release；本地成功不代表该远端流程已完成。

## 本轮实际验证

JDK25.0.1+8；测试仅在进程作用域设置非headless环境，打包时移除JAVA_TOOL_OPTIONS，结束后恢复。

1. 首次命令session16413在测试前失败：PowerShell将未加引号的版本参数拆成`-PappVersion=3`和`.2.3`，后者被Gradle当作任务名。通过PowerShell解析树确认，改为完整字符串`'-PappVersion=3.2.3'`；未改源码或构建配置。
2. 功能分支session13283：`test '-PappVersion=3.2.3' --rerun-tasks --no-daemon --console=plain`，native exit0，1m35s，8任务全部执行；实际XML为161套件、1567总计、1564通过、0失败/错误、3既有跳过。
3. 快进后的main在session9494执行相同完整测试，1m36s、8任务全部执行。独立读取main实际XML同为1564通过/3跳过/0失败错误。
4. 随后在main执行`jpackageImage '-PappVersion=3.2.3' --no-daemon --console=plain`，33s、14任务（8执行/6最新），整个session9494 native exit0。生成版本资源与正式cfg均为3.2.3，正式入口仍为DataCubeFx，无隔离user.home参数。
5. 新镜像modules SHA256：`D8A41496DA3071A26E38CFF887A467C16F711160BA592559F9F2F837A0EB6B1A`。生产入口/补全/工作区管理类存在；167个测试源映射同名class，镜像匹配0项，无验收launcher/probe。

三个跳过仍是Redis standalone及Oracle/PostgreSQL SchemaDiff真实服务集成测试。既有SqlEditorResultFilterContractTest unchecked编译提示及JEP493工具链提示保留。

## 保留边界

正式入口桌面与重启验收见[正式入口记录](2026-08-31-workspace-formal-entry-acceptance.md)，补全缺陷及独立审查见[桌面续验](2026-08-31-workspace-desktop-followup.md)。本轮新版本镜像未重新执行桌面操作，不冒充新的桌面覆盖；键盘Tab/Shift+Tab/Esc人工确认仍待完成。JavaFX原生访问警告继续跟踪，不因此声称安装、升级和全部键盘路径已验收。

不读取/修改/暂存`.testagent/`，不接触真实配置或数据库；保留独立worktree、合成profile及历史证据。本记录在tag与推送之前提交，最终推送状态以实际远端refs核对结果为准。
