# 执行概览按真实耗时排序

基线 main `9d9d43c`。概览当前把耗时写成 `1000ms` 等字符串，默认列比较器按字符排序，不适合查找慢语句。本轮只修正排序值，不新增入口或调整布局。

- “耗时”仍显示原毫秒值和 `ms`，但按原始 long 数值升序/降序比较，不把字符顺序或 int/double 转换当成时长。
- 同耗时保持既有稳定排序和多列次级排序规则；异常筛选前后保持排序，选中项、结果和详情仍对应原始快照。
- 不更改默认执行顺序、批次总汇、报告记录、结果下拉框、实际 SQL/耗时统计、导出来源或普通查询字段的排序。
- 仅改概览行值，复用现有 JavaFX TableView 排序与默认文本呈现；不更改数据库请求、文件、草稿、历史、系统剪贴板或 `.testagent/`。

步骤：真实 TableView 回归复现错误顺序 → 单控制器最小修复 → 定向/全量测试与开发镜像 → 自审提交并本地快进 main、复验镜像一致性。此为排序逻辑修复，不重试已受限的桌面通道，不将自动布局/事件测试冒充原生交互验收；上一轮 SQL 摘要桌面待验项继续保留。不推送、不打 tag。
