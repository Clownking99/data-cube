# 查询结果恢复原始行序

基线 main `9fa6ae1`。在查询结果表右键菜单增加“恢复原始行序”，仅整理本次查询已加载的可见行，不重新执行 SQL；不增加工具栏控件、持久设置或全局快捷键。

- 清除所有表头排序，以 `resultRowIndexes` 中的原始返回位置排序；不按单元格值、当前行号或相等行内容识别。取消表头箭头但行仍乱序时也可恢复。
- 保持已应用的文本/条件筛选、列顺序/宽度/可见性、多单元格选区与焦点身份。无选择不代选；筛掉的行不补回。待应用的搜索继续走原有流程，不在恢复动作中提交搜索。
- “当前筛选结果”导出跟随恢复后的可见次序，“全部已加载”仍为原加载范围；不改原始结果、SQL、撤销历史或批次选择。
- 空结果或零匹配仍可清除活动排序；无活动排序且顺序未变时禁用。只对 QUERY 可用，执行概览、更新、错误和计划不混用。
- 打开菜单时捕获既有结果视图修订号，动作执行前复核当前查询与行映射；更换查询、筛选重建、离开查询、关闭或执行中拒绝迟到操作。菜单动作不持有旧查询/行列表引用。仅遍历当前已加载数据，不发网络请求。

实现聚焦 `SqlEditorPane` 的既有结果上下文菜单及无持久状态的恢复方法，不增存储/并发逻辑。复用离线面板 fixture，失败先行覆盖身份/筛选/列布局/导出组合、空/单行、无选择、迟到动作和非查询状态；再做关联回归、全量测试、开发打包与隔离原生桌面验收。完成后本地快进 main；本轮不推送、不打 tag，不读取真实连接/SQL/历史/凭据/剪贴板或 `.testagent/`。
